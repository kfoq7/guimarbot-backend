---
const plans = [
  {
    id: 1,
    name: 'Basic plan',
    content: [
      {
        plan: 'Limite de acceso por cursos',
        active: true
      },
      {
        plan: 'Lorem Ipsum is simply dummy text of the printing and typesetting industry.',
        active: false
      }
    ]
  },
  {
    id: 1,
    name: 'Basic plan',
    content: [
      {
        plan: 'Limite de acceso por cursos',
        active: true
      },
      {
        plan: 'Lorem Ipsum is simply dummy text of the printing and typesetting industry.',
        active: false
      }
    ]
  },
  {
    id: 1,
    name: 'Basic plan',
    content: [
      {
        plan: 'Limite de acceso por cursos',
        active: true
      },
      {
        plan: 'Lorem Ipsum is simply dummy text of the printing and typesetting industry.',
        active: false
      }
    ]
  }
]
---

<section class="mt-20 px-4">
  <h1 class="text-center text-2xl font-bold md:text-3xl">
    Elige el plan que mejor se adapte a ti
  </h1>

  <p class="mt-2 text-pretty text-center text-lg">
    Mejora tu oportunidad de crecimiento personal y profesional.
  </p>

  <div
    class="mt-14 flex flex-col items-center justify-center gap-x-5 gap-y-4 md:flex-row"
  >
    {
      plans.map(({ name, content }) => (
        <div class="w-full rounded-md border-2 border-gray-800/80 bg-midnight-navy px-5 py-3 drop-shadow-md md:w-96">
          <div class="flex items-center justify-between">
            <h1 class="text-left text-xl font-semibold text-white md:text-2xl">
              {name}
            </h1>

            <span class="rounded-full border border-black px-2.5 py-0.5 text-gray-200/80 backdrop-blur-sm">
              Mensual
            </span>
          </div>

          <p class="mt-4 text-white">
            Para <span class="font-bold">1 estudiante</span>
          </p>

          <ul class="my-4 flex flex-col gap-y-3 text-white">
            {content.map(({ plan, active }) => (
              <ol class:list={['', { 'text-gray-100/30': active }]}>{plan}</ol>
            ))}
          </ul>

          <div class="mt-4 w-full">
            <a
              href="/auth/login"
              class="block w-full rounded-md border border-blue-600 bg-blue-800 px-3 py-2 text-center text-white transition-all duration-300 hover:scale-105"
            >
              Adquirir
            </a>
          </div>
        </div>
      ))
    }
  </div>
</section>
